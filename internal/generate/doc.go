// Package generate transforms model and interface metadata into template-ready
// query structures. It parses DIY SQL sections, resolves field types and
// relationships, and renders the model and query artifacts requested by gen.
package generate
