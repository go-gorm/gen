// Package template contains the text templates used to render generated model,
// query, interface, and test files. Templates operate on normalized metadata
// from the internal model, parser, and generate packages.
package template
